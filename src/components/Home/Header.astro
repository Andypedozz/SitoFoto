---
const { links } = Astro.props;
---
<nav>
    <a class="nav-logo" href="#">UG<span>ACTION</span>&nbsp;MEDIA</a>
    <ul class="nav-links">
        {links.map((link: any) => (
            <li><a href={link.href}>{link.name}</a></li>
        ))}
    </ul>
    <a class="nav-cta" href="#contact">Prenota ora</a>
</nav>
